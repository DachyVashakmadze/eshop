import { CoolHoverDirective } from './cool-hover.directive';

describe('CoolHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CoolHoverDirective();
    expect(directive).toBeTruthy();
  });
});
